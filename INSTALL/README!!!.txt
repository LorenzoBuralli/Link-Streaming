ITA

Prima di avviare l'applicazione, se non hai già installato Python sul tuo sistema operativo, utilizza "Py 3.11.3 + Lib Auto Install.bat" per avviare un installazione automatica dell'interprete e le due librerie necessarie all'esecuzione del programma.
Se desideri disintallarlo, utilizza l'altro script "Unistall.bat" per avviare una disinstallazione pulita di Python 3.11.3 e le due librerie.
Volendo puoi verificare le versioni installate con "Verify.bat".

Problemi noti:
-Il programma non funziona correttamente senza la versione Python 3.11.3;
-Il programma non funziona correttamente senza la versione delle librerie riportate in "requirements.txt";
-Il programma non funziona correttamente senza la versione di "chromedriver.exe" compatibile con quella del browser Chrome. Scaricabile da "https://chromedriver.chromium.org/downloads" se necessita di essere aggiornata.


ENG

Before starting the application, if you haven't already installed Python on your operating system, use "Py 3.11.3 + Lib Auto Install.bat" to start an automatic installation of the interpreter and the two libraries needed to run the program.
If you wish to uninstall it, use the other "Unistall.bat" script to initiate a clean uninstall of Python 3.11.3 and the two libraries.
If you wish, you can verify the installed versions with "Verify.bat".

Known Issues:
-The program doesn't work properly without Python version 3.11.3;
-The program doesn't work correctly without the version of the libraries listed in "requirements.txt";
-The program does not work properly without the version of "chromedriver.exe" compatible with that of the Chrome browser. Downloadable from "https://chromedriver.chromium.org/downloads" if it needs to be updated.

---------

Ryan119
Version 1.0